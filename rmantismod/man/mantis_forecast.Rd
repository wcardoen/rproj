% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{mantis_forecast}
\alias{mantis_forecast}
\title{Forecast with Mantis}
\usage{
mantis_forecast(
  time_series,
  model_dir,
  python_exe,
  covariate = NULL,
  target_type = 2L,
  covariate_type = NULL,
  horizon = 4L,
  use_covariate = TRUE,
  device = "cuda",
  debug = FALSE
)
}
\arguments{
\item{time_series}{Numeric vector of observed counts (required).}

\item{model_dir}{Character. Directory containing the pretrained \code{.pt}
weight files (required). Use \code{\link{mantis_download_weights}} to
populate this directory.}

\item{python_exe}{Character. Path to the Python executable (required).}

\item{covariate}{Optional numeric vector of covariate values aligned with
\code{time_series}. Pass \code{NULL} (default) when no covariate is used.}

\item{target_type}{Integer. Type of target series:
\code{0} = cases, \code{1} = hospitalisations, \code{2} = deaths (default).}

\item{covariate_type}{Integer or \code{NULL}. Type code for the covariate
series; required when \code{covariate} is non-\code{NULL}.}

\item{horizon}{Integer forecast horizon; must be \code{4} or \code{8}.
Default is \code{4}.}

\item{use_covariate}{Logical. Whether to load the covariate-enabled model
weights. Default is \code{TRUE}.}

\item{device}{Character. The compute device to use; must be \code{"cuda"}
or \code{"cpu"}. Default is \code{"cuda"}.}

\item{debug}{Logical. When \code{TRUE}, prints diagnostic messages about
the Python executable and model directory being used. Default is
\code{FALSE}.}
}
\value{
A numeric matrix of forecast samples returned by the Mantis model.
}
\description{
Runs the Mantis deep-learning forecast model on a numeric time series,
optionally using a covariate series. Python and the \code{mantis} module
are validated before any calls are made via \code{mantis_check_python()}.
}
\seealso{
\code{\link{mantis_download_weights}} to fetch the required weights.
}
